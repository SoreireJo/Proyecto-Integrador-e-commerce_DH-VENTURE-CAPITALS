<!DOCTYPE html>
<html lang="es">
  <!-- Head -->
  <%- include('../partials/head.ejs') %>
  <!-- /Head -->

    <body>
        <!-- header -->
        <%- include('../partials/header.ejs') %>

        <main class="container-main">
            <section class="container-users">
              
              <form class="users" action="">
                <fieldset class="login-access">
                  <legend>Iniciar sesión</legend>
                  <label for="user">Usuario: </label>
                  <input
                    type="text"
                    name="user"
                    id="user"
                    pattern="[a-z,A-Z,À-ÿ,\s,\S,\d,\-]{7,60}"
                    placeholder="Ingresar usuario"
                    required
                    title="Debes ingresar un nombre de usuario  por ejemplo: sleep_q ; no se aceptan caracteres especiales (@,*´+)"
                  />
                  <label for="password">Contraseña: </label>
                  <input
                    type="password"
                    name="password"
                    id="password"
                    pattern="(?=.*\d)(?=.*[a-z])(?=.*[A-Z])\w\S{6,}"
                    placeholder="Ingresar contraseña"
                    title="Requisitos mínimos:
                    6 caracteres, una mayúscula y una minúscula.
                    Puede usar caracteres especiales (*/.}{¿'=, etc..)."
                    required
                  />
                </fieldset>
                
                <div class="linea"></div>
                
                <div class="container-buttons">
                  <button class="container-buttons" type="submit" id="button-in">Ingresar</button>
                </div>

                <div class="linea"></div>
                <div class="media-linea"></div>

                <div class="container-link">
                  <p id="link"><a href="#"><span id="FrgtPass">¿olvidaste tu contraseña?</span></a></p>
                </div>

                <div class="linea"></div>
                <div class="media-linea"></div>

                <div class="container-link">
                  <p id="link"><a href="/register"> ¿No tenés una cuenta?<span id="IniSesion"> Registrate</span></a></p>
                </div>
              </form>
            </section>
        </main>

        <!-- footer -->
        <%- include('../partials/footer.ejs') %>
    </body>
</html>